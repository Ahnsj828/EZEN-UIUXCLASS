const Header = () => {
  return (
    <header>
      <h1>header</h1>
    </header>
  );
};

export default Header;
// 헤더라는 컴포넌트 데이터를 기본값으로 export하겠다 => 다른데로 가져다 쓸 수 있다
// default 이 컴포넌트 내에서는 하나만 가져가겠다라는거다

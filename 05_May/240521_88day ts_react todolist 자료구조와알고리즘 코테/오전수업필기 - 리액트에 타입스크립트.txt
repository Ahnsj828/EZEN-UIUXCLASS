< atom값을 가져오는 사용하는 함수 >
useRecoilValue : atom의 값을 가져와서 사용
useSetRecoilState : atom의 값을 변형.편집해서 사용
useRecoilState : atom의 값을 가져와서 변형.편집


TodoList에 입력되어야 하는 값에 대한 정의 
(항상 CRUD 생각하기)
- id : number
id값이 필요하고 
- text: string
어떤 내용을 입력할지에 대한 text가 필요하고
- category : 
category가 필요하다



배열안에 있는 값들을 찾아오는 함수가 여러개 있었다!!
그 중에서 findIndex( ) => 하는역할: 배열안에 있는 값들 가운데서 특정 조건을 만족하는 
여러개의 요소들 가운데 가장 첫번째 요소의 인덱스값을 반환!!


